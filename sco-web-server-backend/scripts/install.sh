# For script start, should run in a terminal in the same folder that the file: `./install.sh`
echo "Installing sco nodeserver application in production environment"
npm i
echo "Installing sco nodeserver application in production environment finished"